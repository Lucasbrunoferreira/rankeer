import styled from 'styled-components/native';

const ListEvaluations = styled.ScrollView`
  margin-bottom: 20px;
  padding: 10px 0;
`;

const BtnsWrapper = styled.View`
  flex-direction: column;
`;

export default {
  ListEvaluations,
  BtnsWrapper,
}
