import React from 'react';
import Styles from './styles';

const SideMenu: React.FC = () => {
  return (
    <Styles.SideMenu>
      side menu
    </Styles.SideMenu>
  );
};

export default SideMenu;
